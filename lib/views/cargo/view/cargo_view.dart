import 'package:flutter/material.dart';

class CargoView extends StatelessWidget {
  const CargoView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Kargo Bilgileri Sayfası'),
      alignment: Alignment.center,
    );
  }
}
